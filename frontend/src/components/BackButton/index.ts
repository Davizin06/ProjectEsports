import styled from 'styled-components'

export const BackButton = styled.button`
    margin-bottom: 16px;
    border: none;
    background: #222;
    color: white;
    padding: 10px 16px;
    border-radius: 6px;
    cursor: pointer;
    font-weight: 600;

    &:hover {
        background: #444;
    }
`
